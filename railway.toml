[build]
buildCommand = "pip install -r restaurant_kds_project/requirements.txt"

[deploy]
startCommand = "cd restaurant_kds_project && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
